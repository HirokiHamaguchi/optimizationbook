This is a optimization and sampling textbook by Yin Tat Lee and Santosh Vempala.

This is far from done.

See [main.pdf](https://github.com/YinTat/optimizationbook/raw/main/main.pdf) for the current version.

-------

✅ ToDo: compile lyx to generate tex file. (convexity.tex)

* P.7 In Sec.1.1, the region where f is not $\epsilon$ has volume <font color="red">$\frac{1}{\epsilon^n}$</font> times the volume of the unit ball,
  * This should be <font color="blue">$\epsilon^n$</font>.

* P.12 In Sec.1.3.3, there are some <font color="red">$x^i$</font>.
  * This should be <font color="blue">$x_i$</font>.

* P.13 In Def.1.14, The epigraph of $f : \mathbb{R}^n \to$ <font color="red">$\mathbb{R}$</font> is $\mathrm{epi} f\coloneqq\{(x,t)\in\mathbb{R}^n\times\mathbb{R}:\ t\geq f(x)\}$
  * This should be <font color="blue">$\mathbb{R}\cup\{+\infty\}$</font>.

* P.13 In Ex.1.18, the function $h(x) =$ <font color="red">$\max f(x), g(x)$</font> is also convex.
  * This should be <font color="blue">$\max\{f(x), g(x)\}$</font>.

* P.15 In Ex.1.21, there are some <font color="red">$x^i$</font> and <font color="red">$y^i$</font>.
  * This should be <font color="blue">$x_i$</font> and <font color="blue">$y_i$</font>.
